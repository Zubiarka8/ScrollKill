# Shell scripts must keep LF endings even when checked out on Windows (core.autocrlf=true),
# or bash chokes on the trailing CR. Added with the D3 profiling harness (scripts/profiling/).
*.sh text eol=lf
